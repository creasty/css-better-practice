L1
==

ここは `L1` です。

この階層には、以下の様なディレクトリがあります。

```
|-- components/  --+
|-- headers/       | <-- 基本単位
|-- shared/      --+
|-- vendor/      <------ ベンダーの CSS (プラグイン等)
`-- layouts/     <------ 各レイアウト (テーマ) 毎のスタイル
```
